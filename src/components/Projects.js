import { Typography, Box } from "@mui/material";
import hotdog from "../images/hotdog.svg"

export const Projects = () => {
  return (
    <Box sx={{ display: "flex", width: "99.9vw", border: 1, ml: -3 }}>
      <Typography sx={{}}>testing from project page</Typography>
    // </Box>
   
  );
};
