import { Typography, Box } from "@mui/material";

export const Projects = () => {
  return (
    <Box sx={{ display: "flex", width: "100vw", border: 1, ml: -4 }}>
      <Typography sx={{}}>testing from project page</Typography>
    </Box>
  );
};
